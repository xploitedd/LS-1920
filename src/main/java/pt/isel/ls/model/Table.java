package pt.isel.ls.model;

import java.util.HashMap;
import java.util.LinkedList;
import java.util.Set;

public class Table {
    private String[][] Columns;

    public Table(int cSize, int rSize) {
        Columns = new String[cSize][rSize];
    }

    public void addColumn(String columnName, LinkedList<String> values) {
        return;
    }
}
